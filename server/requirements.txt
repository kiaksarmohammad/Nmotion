
# =============================================================================
# NEUROMOTION AI - BACKEND DEPENDENCIES
# =============================================================================
#
# INSTALLATION (PowerShell):
#
# 1. Basic deps:
#    pip install -r requirements.txt
#
# 2. PyTorch (RTX 5080 / Blackwell needs CUDA 12.8+ wheels):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#    Verify: python -c "import torch; print(torch.cuda.is_available())"
#
# =============================================================================

# Core Web & AI
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
numpy>=1.24.0
scipy>=1.10.0
google-genai>=0.3.0
opencv-python-headless>=4.8.0

# Streamlit UI (replaces React/TypeScript frontend)
streamlit>=1.40.0
plotly>=5.18.0

# YOLO26x-Pose (replaces OpenMMLab ViTPose stack)
ultralytics>=8.3.0

# RL Pipeline
mlflow>=2.0.0
stable-baselines3>=2.0.0
pettingzoo>=1.24.0
shimmy>=1.3.0              # PettingZoo ↔ Gymnasium bridge
shap>=0.44.0
imbalanced-learn>=0.12.0
tslearn>=0.6.0
pandas>=2.0.0
scikit-learn>=1.4.0
gymnasium>=0.29.0
supersuit>=3.9.0           # PettingZoo vectorization wrappers

# HARDWARE DEPENDENT LIBRARIES
# Install manually using Step 2 above — not listed here to prevent
# pip from pulling incompatible CPU-only versions.
#
# torch
# torchvision
